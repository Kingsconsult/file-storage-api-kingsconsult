setup:
  addons:
    - plan: mongolab:sandbox
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - python file_storage/manage.py migrate

run:
  web: python file_storage/manage.py runserver 0.0.0.0:$PORT


